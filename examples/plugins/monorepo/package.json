{
  "name": "monorepo",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun src/index.ts",
    "start": "bun src/index.ts",
    "generate:skills": "bun scripts/generate-skills-registry.ts",
    "build:worker": "bun run generate:skills && bun build worker.ts --outdir dist-worker --target browser",
    "demo": "bun test/smoke.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "@peri-code/mcpp": "workspace:*"
  }
}