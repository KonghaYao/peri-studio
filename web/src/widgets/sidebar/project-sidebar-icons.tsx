import {
  Archive,
  Download,
  MoreHorizontal,
  Pencil,
  Plus,
} from 'lucide-solid';

export function PlusIcon() { return <Plus size={15} strokeWidth={1.7} />; }
export function ImportIcon() { return <Download size={16} strokeWidth={1.7} />; }
export function MoreIcon() { return <MoreHorizontal size={14} strokeWidth={1.7} />; }
export function RenameIcon() { return <Pencil size={16} strokeWidth={1.7} />; }
export function ArchiveIcon() { return <Archive size={16} strokeWidth={1.7} />; }
