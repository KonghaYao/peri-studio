# AI Elements reference (local only)

Upstream: https://github.com/vercel/ai-elements

Refresh:

```bash
./scripts/download-ai-elements-reference.sh
```

Compare Solid ports under `packages/ui/src/components/` with
`.tmp/ai-elements-reference/packages/elements/src/`.
