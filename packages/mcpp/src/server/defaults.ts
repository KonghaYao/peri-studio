/** MCPP HTTP 承载的单一默认监听地址与端口。 */
export const DEFAULT_MCPP_HTTP_HOST = "127.0.0.1";
export const DEFAULT_MCPP_HTTP_PORT = 8457;

/** MCPP Response Cache 与 Resource Content Cache 的默认 TTL：1 天。 */
export const DEFAULT_MCPP_CACHE_TTL_MS = 24 * 60 * 60 * 1_000;
