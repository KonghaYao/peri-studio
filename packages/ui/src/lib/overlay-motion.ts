const motionSafe = 'motion-reduce:animate-none';

/** 遮罩层：淡入淡出。 */
export const overlayScrimMotion = `ui-overlay-scrim-motion ${motionSafe}`;

/** 居中模态框（Dialog / AlertDialog）。 */
export const modalDialogMotion = `ui-modal-dialog-motion ${motionSafe}`;

/** 锚定浮层（Popover / HoverCard / DatePicker 等）。 */
export const surfacePopoverMotion = `ui-surface-popover-motion ${motionSafe}`;

/** 侧滑 / 底滑面板（Sheet / Drawer）— 与方向 slide 类组合。 */
export const panelSheetMotion = `ui-panel-sheet-motion ${motionSafe}`;

/** 菜单 / Select / Combobox 等内容面。 */
export const menuSurfaceMotion = `ui-menu-surface-motion ${motionSafe}`;

/** Tooltip 内容。 */
export const tooltipContentMotion = `ui-tooltip-motion ${motionSafe}`;

/** 可折叠内容区（Reasoning / Tool / ChainOfThought）。 */
export const disclosureContentMotion = `ui-disclosure-motion ${motionSafe}`;

/** Toast 通知（Kobalte data-opened / data-closed）。 */
export const toastContentMotion = `ui-toast-motion ${motionSafe}`;
